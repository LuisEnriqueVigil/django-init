{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:holamundo}/db.sqlite3",
            "name": "django"
        }
    ]
}